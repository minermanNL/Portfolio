specVersion: "v1"
serviceId: "studio"
location: "europe-west1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "postgres"
      cloudSql:
        instanceId: "timexai-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
